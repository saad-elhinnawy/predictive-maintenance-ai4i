{
  "name": "bmw-export",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "NODE_ENV=development npm --prefix backend install && NODE_ENV=development npm --prefix backend run build",
    "start": "npm --prefix backend start"
  }
}
